# Couchbase Ansible Role Examples

### Simple

```bash
ansible-playbook --private-key ~/.ssh/mykey.pem -i hosts simple.yml
```

### Full

```bash
ansible-playbook --private-key ~/.ssh/mykey.pem -i hosts full.yml
```